.message-shell {
  background:
    radial-gradient(circle at 50% 0%, rgba(255, 190, 107, 0.24), transparent 30%),
    radial-gradient(circle at 0% 18%, rgba(47, 114, 219, 0.08), transparent 28%),
    linear-gradient(180deg, #fff5e9 0%, #f7f8fc 34%, #f4f6fb 100%);
}

.message-page {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
  padding: 18rpx 36rpx 44rpx;
}

.message-hero {
  position: relative;
  min-height: 108rpx;
  overflow: hidden;
  display: flex;
  align-items: center;
  gap: 16rpx;
  padding: 24rpx 26rpx;
  border-radius: 26rpx;
  border: 1rpx solid rgba(255, 122, 0, 0.2);
  background:
    linear-gradient(112deg, rgba(255, 255, 255, 0.98) 0%, rgba(255, 250, 244, 0.96) 58%, rgba(255, 233, 209, 0.66) 100%),
    #ffffff;
  box-shadow: 0 8rpx 20rpx rgba(210, 104, 18, 0.045);
}

.message-hero::after {
  content: "";
  position: absolute;
  right: -34rpx;
  bottom: -48rpx;
  width: 180rpx;
  height: 180rpx;
  border-radius: 50%;
  background: rgba(255, 122, 0, 0.08);
}

.message-hero__copy {
  position: relative;
  z-index: 2;
  flex: 1;
  min-width: 0;
}

.message-hero__title {
  color: #111827;
  font-size: 28rpx;
  font-weight: 500;
  line-height: 1.15;
}

.message-hero__desc {
  margin-top: 6rpx;
  color: #5f6676;
  font-size: 20rpx;
  line-height: 1.35;
}

.message-bell {
  position: absolute;
  right: 118rpx;
  top: 28rpx;
  width: 50rpx;
  height: 48rpx;
  transform: rotate(-8deg);
  opacity: 0.46;
}

.message-bell__body {
  position: absolute;
  left: 10rpx;
  top: 8rpx;
  width: 34rpx;
  height: 35rpx;
  border-radius: 26rpx 26rpx 15rpx 15rpx;
  background: linear-gradient(145deg, #ffd9b5 0%, #ff8a16 100%);
  box-shadow: 0 7rpx 14rpx rgba(255, 122, 0, 0.1);
}

.message-bell__body::before {
  content: "";
  position: absolute;
  left: 13rpx;
  top: -6rpx;
  width: 11rpx;
  height: 11rpx;
  border-radius: 50%;
  background: #ff9b32;
}

.message-bell__body::after {
  content: "";
  position: absolute;
  left: -5rpx;
  right: -5rpx;
  bottom: -3rpx;
  height: 9rpx;
  border-radius: 999rpx;
  background: linear-gradient(90deg, #ffc796 0%, #ff7900 100%);
}

.message-bell__clapper {
  position: absolute;
  left: 25rpx;
  bottom: 5rpx;
  width: 8rpx;
  height: 8rpx;
  border-radius: 50%;
  background: #ff7a00;
}

.message-count {
  position: relative;
  z-index: 2;
  flex-shrink: 0;
  min-width: 78rpx;
  padding: 7rpx 14rpx;
  border-radius: 999rpx;
  color: #ff6f00;
  background: rgba(255, 250, 244, 0.8);
  border: 1rpx solid rgba(255, 122, 0, 0.18);
  text-align: center;
  font-size: 22rpx;
  font-weight: 500;
}

.message-list {
  display: flex;
  flex-direction: column;
  gap: 10rpx;
}

.message-card {
  position: relative;
  min-height: 88rpx;
  display: flex;
  align-items: center;
  gap: 18rpx;
  padding: 18rpx 48rpx 18rpx 22rpx;
  border-radius: 24rpx;
  background: rgba(255, 255, 255, 0.98);
  border: 1rpx solid rgba(31, 36, 50, 0.045);
  box-shadow: 0 7rpx 18rpx rgba(24, 34, 56, 0.035);
}

.message-card::before {
  content: "";
  position: absolute;
  left: 0;
  top: 18rpx;
  bottom: 18rpx;
  width: 5rpx;
  border-radius: 999rpx;
  background: rgba(255, 122, 0, 0.42);
}

.message-icon {
  position: relative;
  width: 44rpx;
  height: 44rpx;
  flex-shrink: 0;
  border-radius: 50%;
  color: #ff7900;
  background: linear-gradient(180deg, #fff4e7, #ffe8d1);
  box-sizing: border-box;
}

.message-icon--paid,
.message-icon--finish {
  color: #18a76f;
  background: linear-gradient(180deg, #eafff5, #d8f7e8);
}

.message-icon--start,
.message-icon--car,
.message-icon--driver {
  color: #2f72db;
  background: linear-gradient(180deg, #edf5ff, #ddeaff);
}

.message-icon--cancel::before,
.message-icon--cancel::after {
  content: "";
  position: absolute;
  left: 50%;
  top: 50%;
  width: 17rpx;
  height: 4rpx;
  border-radius: 999rpx;
  background: currentColor;
}

.message-icon--cancel::before {
  transform: translate(-50%, -50%) rotate(45deg);
}

.message-icon--cancel::after {
  transform: translate(-50%, -50%) rotate(-45deg);
}

.message-icon--order::before {
  content: "";
  position: absolute;
  left: 50%;
  top: 50%;
  width: 19rpx;
  height: 25rpx;
  border-radius: 7rpx;
  background: currentColor;
  transform: translate(-50%, -50%);
}

.message-icon--order::after {
  content: "";
  position: absolute;
  left: 50%;
  top: 50%;
  width: 9rpx;
  height: 2rpx;
  border-radius: 999rpx;
  background: #ffffff;
  box-shadow: 0 7rpx 0 #ffffff;
  transform: translate(-50%, -120%);
}

.message-icon--paid::before,
.message-icon--finish::before {
  content: "";
  position: absolute;
  left: 50%;
  top: 50%;
  width: 13rpx;
  height: 23rpx;
  border-right: 4rpx solid currentColor;
  border-bottom: 4rpx solid currentColor;
  box-sizing: border-box;
  transform: translate(-35%, -58%) rotate(42deg);
  transform-origin: center;
}

.message-icon--finish::before {
  width: 12rpx;
  height: 21rpx;
}

.message-icon--start::before {
  content: "";
  position: absolute;
  left: 50%;
  top: 50%;
  border-left: 16rpx solid currentColor;
  border-top: 10rpx solid transparent;
  border-bottom: 10rpx solid transparent;
  transform: translate(-34%, -50%);
}

.message-icon--car::before {
  content: "";
  position: absolute;
  left: 50%;
  top: 50%;
  width: 26rpx;
  height: 14rpx;
  border-radius: 10rpx 12rpx 6rpx 6rpx;
  border: 3rpx solid currentColor;
  box-sizing: border-box;
  transform: translate(-50%, -10%);
}

.message-icon--car::after {
  content: "";
  position: absolute;
  left: 50%;
  top: 50%;
  width: 5rpx;
  height: 5rpx;
  border-radius: 50%;
  background: currentColor;
  box-shadow: 13rpx 0 0 currentColor, 5rpx -17rpx 0 -1rpx currentColor;
  transform: translate(-162%, 76%);
}

.message-icon--driver::before,
.message-icon--driver::after {
  content: "";
  position: absolute;
  border-radius: 50%;
  background: currentColor;
}

.message-icon--driver::before {
  left: 50%;
  top: 50%;
  width: 13rpx;
  height: 13rpx;
  transform: translate(-50%, -86%);
}

.message-icon--driver::after {
  left: 50%;
  top: 50%;
  width: 22rpx;
  height: 14rpx;
  border-radius: 18rpx 18rpx 7rpx 7rpx;
  transform: translate(-50%, 16%);
}

.message-icon--notice::before {
  content: "";
  position: absolute;
  left: 50%;
  top: 50%;
  width: 20rpx;
  height: 20rpx;
  border-radius: 50%;
  border: 3rpx solid currentColor;
  box-sizing: border-box;
  transform: translate(-50%, -50%);
}

.message-icon--notice::after {
  content: "";
  position: absolute;
  left: 50%;
  top: 50%;
  width: 4rpx;
  height: 10rpx;
  border-radius: 999rpx;
  background: currentColor;
  transform: translate(-50%, -20%);
}

.message-card__main {
  flex: 1;
  min-width: 0;
}

.message-card__title {
  color: #111827;
  font-size: 23rpx;
  line-height: 1.25;
  font-weight: 500;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.message-card__content {
  margin-top: 5rpx;
  color: #222733;
  font-size: 20rpx;
  line-height: 1.45;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.message-card__time {
  margin-top: 6rpx;
  color: #8b94a7;
  font-size: 20rpx;
}

.message-card__dot {
  position: absolute;
  left: 50rpx;
  top: 16rpx;
  width: 10rpx;
  height: 10rpx;
  border-radius: 50%;
  background: #ff5b57;
  box-shadow: 0 0 0 5rpx #ffffff;
}

.message-arrow {
  position: absolute;
  right: 22rpx;
  top: 50%;
  width: 15rpx;
  height: 15rpx;
  border-top: 3rpx solid rgba(31, 36, 50, 0.46);
  border-right: 3rpx solid rgba(31, 36, 50, 0.46);
  transform: translateY(-50%) rotate(45deg);
}

.message-empty {
  min-height: 220rpx;
  padding: 36rpx 22rpx;
  border-radius: 26rpx;
  background: rgba(255, 255, 255, 0.98);
  border: 1rpx solid rgba(31, 36, 50, 0.05);
  box-shadow: 0 10rpx 24rpx rgba(24, 34, 56, 0.045);
  text-align: center;
  color: #8a94a8;
}

.message-empty__mark {
  margin: 0 auto 14rpx;
}

.message-empty__title {
  color: #1f2432;
  font-size: 25rpx;
  font-weight: 500;
}

.message-empty__desc {
  margin-top: 7rpx;
  font-size: 21rpx;
  line-height: 1.45;
}

.tap-scale:active {
  transform: translateY(2rpx) scale(0.99);
  opacity: 0.96;
}
