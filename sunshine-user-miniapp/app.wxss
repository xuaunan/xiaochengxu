:root,
page {
  --brand: #ff7a00;
  --brand-soft: #fff1df;
  --brand-deep: #ff9326;
  --brand-warm: #ffb14a;
  --brand-weak: #fff8ef;
  --brand-ring: rgba(255, 122, 0, 0.16);
  --text-primary: #1f2432;
  --text-secondary: #6b7385;
  --text-muted: #98a1b3;
  --text-inverse: #ffffff;
  --line: rgba(31, 36, 50, 0.08);
  --line-strong: rgba(31, 36, 50, 0.12);
  --card: #ffffff;
  --page: #f7f8fc;
  --page-warm: #fff8f2;
  --success: #1ba97a;
  --warning: #ffb020;
  --danger: #ff5b57;
  --info: #2c63ff;
  --shadow-soft: 0 10rpx 24rpx rgba(24, 34, 56, 0.07);
  --shadow-card: 0 14rpx 32rpx rgba(24, 34, 56, 0.08);
  --shadow-brand: 0 12rpx 26rpx rgba(255, 122, 0, 0.16);
  --motion-smooth: cubic-bezier(0.2, 0.8, 0.2, 1);
  --motion-press: cubic-bezier(0.18, 0.72, 0.24, 1);
  --radius-sm: 16rpx;
  --radius-md: 22rpx;
  --radius-lg: 28rpx;
  --radius-xl: 34rpx;
  background: var(--page);
  color: var(--text-primary);
  font-size: 28rpx;
  font-family: "PingFang SC", "Helvetica Neue", sans-serif;
}

view,
text,
button,
input,
textarea,
scroll-view,
swiper,
swiper-item,
map {
  box-sizing: border-box;
}

input {
  min-height: 76rpx;
}

.search-input,
.login-input,
.auth-input {
  width: 100%;
  height: 76rpx;
  min-height: 76rpx;
  padding: 0 24rpx;
  border-radius: 24rpx;
  background: rgba(255, 255, 255, 0.98);
  border: 2rpx solid rgba(255, 122, 0, 0.1);
  box-shadow: 0 10rpx 24rpx rgba(24, 34, 56, 0.055);
  font-size: 28rpx;
  line-height: 76rpx;
  color: var(--text-primary);
}

.search-input::placeholder,
.login-input::placeholder,
.auth-input::placeholder {
  color: var(--text-muted);
}

.page-shell {
  min-height: 100vh;
  background:
    radial-gradient(circle at 92% 0%, rgba(255, 193, 117, 0.14), transparent 34%),
    linear-gradient(180deg, var(--page-warm) 0%, #f7f8fc 32%, #f5f7fb 100%);
}

.safe-bottom {
  padding-bottom: calc(env(safe-area-inset-bottom) + 40rpx);
}

.page-content {
  padding: 24rpx 24rpx 40rpx;
}

.page-hero {
  position: relative;
  overflow: hidden;
  padding: 30rpx 28rpx;
  border-radius: var(--radius-xl);
  background:
    radial-gradient(circle at 100% 0%, rgba(255, 122, 0, 0.08), transparent 34%),
    rgba(255, 255, 255, 0.96);
  color: var(--text-primary);
  border: 1rpx solid rgba(31, 36, 50, 0.05);
  box-shadow: var(--shadow-card);
}

.page-hero::after {
  display: none;
}

.page-hero__kicker,
.page-kicker {
  font-size: 22rpx;
  font-weight: 700;
  letter-spacing: 1rpx;
}

.page-hero__title,
.page-title {
  margin-top: 10rpx;
  font-size: 40rpx;
  font-weight: 800;
  line-height: 1.22;
}

.page-hero__desc,
.page-desc {
  margin-top: 12rpx;
  font-size: 24rpx;
  line-height: 1.65;
}

.page-hero__desc {
  max-width: 580rpx;
  color: var(--text-secondary);
}

.page-header {
  padding: 10rpx 4rpx 18rpx;
}

.page-kicker {
  color: var(--brand);
}

.page-title {
  color: var(--text-primary);
}

.page-desc {
  color: var(--text-secondary);
}

.home-page {
  position: relative;
  min-height: 100vh;
  background: #f7f8fc;
}

.home-map {
  width: 100%;
  height: 100vh;
}

.home-overlay {
  position: absolute;
  left: 0;
  right: 0;
  top: 0;
  z-index: 10;
}

.hero-card,
.panel-card,
.glass-card {
  background: rgba(255, 255, 255, 0.96);
  border: 1rpx solid rgba(31, 36, 50, 0.05);
  border-radius: 32rpx;
  box-shadow: var(--shadow-card);
}

.hero-card {
  padding: 28rpx;
}

.panel-card {
  padding: 24rpx;
}

.glass-card {
  background: rgba(255, 255, 255, 0.92);
  backdrop-filter: blur(12rpx);
  padding: 24rpx;
}

.section-title {
  font-size: 36rpx;
  font-weight: 800;
  color: var(--text-primary);
  line-height: 1.28;
}

.section-subtitle {
  margin-top: 10rpx;
  font-size: 24rpx;
  line-height: 1.65;
  color: var(--text-secondary);
}

.grid-2 {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 20rpx;
}

.row-between {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.row-center {
  display: flex;
  align-items: center;
}

.text-primary {
  color: var(--text-primary);
}

.text-secondary {
  color: var(--text-secondary);
}

.text-muted {
  color: var(--text-muted);
}

.brand-text {
  color: var(--brand);
}

.pill {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 10rpx 20rpx;
  border-radius: 999rpx;
  font-size: 22rpx;
  background: var(--brand-soft);
  color: var(--brand);
  line-height: 1.2;
}

.soft-pill {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 8rpx 16rpx;
  border-radius: 999rpx;
  font-size: 21rpx;
  font-weight: 700;
  color: var(--brand);
  background: rgba(255, 122, 0, 0.1);
}

.divider {
  height: 1rpx;
  background: var(--line);
}

.primary-button,
.ghost-button,
.warn-button {
  border: none;
  border-radius: 24rpx;
  font-size: 30rpx;
  font-weight: 600;
  line-height: 1;
  padding: 26rpx 28rpx;
  transition:
    transform 0.16s var(--motion-press),
    opacity 0.16s var(--motion-press),
    box-shadow 0.22s var(--motion-smooth),
    background 0.22s var(--motion-smooth);
  will-change: transform, opacity;
}

.primary-button::after,
.ghost-button::after,
.warn-button::after {
  border: none;
}

.primary-button {
  color: #ffffff;
  background: linear-gradient(135deg, var(--brand) 0%, var(--brand-deep) 100%);
  box-shadow: 0 10rpx 24rpx rgba(255, 122, 0, 0.16);
}

.ghost-button {
  color: var(--brand);
  background: rgba(255, 122, 0, 0.1);
}

.warn-button {
  color: #ffffff;
  background: linear-gradient(135deg, #ff7f68 0%, #ff5b57 100%);
}

.primary-button.button-hover,
.ghost-button.button-hover,
.warn-button.button-hover,
.tap-scale:active,
.pressable-hover {
  transform: translateY(2rpx) scale(0.985);
  opacity: 0.96;
}

.field-card {
  display: flex;
  align-items: center;
  gap: 20rpx;
  padding: 22rpx 24rpx;
  background: rgba(255, 255, 255, 0.96);
  border: 1rpx solid rgba(31, 36, 50, 0.05);
  border-radius: 24rpx;
  box-shadow: var(--shadow-soft);
}

.form-card {
  padding: 24rpx;
  border-radius: 28rpx;
  background: rgba(255, 255, 255, 0.96);
  border: 1rpx solid rgba(31, 36, 50, 0.05);
  box-shadow: var(--shadow-soft);
}

.form-label {
  margin-bottom: 12rpx;
  font-size: 24rpx;
  font-weight: 700;
  color: var(--text-primary);
}

.form-help {
  margin-top: 10rpx;
  font-size: 22rpx;
  line-height: 1.55;
  color: var(--text-muted);
}

.list-card {
  position: relative;
  overflow: hidden;
  padding: 24rpx;
  border-radius: 30rpx;
  background: rgba(255, 255, 255, 0.97);
  border: 1rpx solid rgba(31, 36, 50, 0.05);
  box-shadow: var(--shadow-soft);
}

.list-card::before {
  content: "";
  position: absolute;
  left: 0;
  top: 22rpx;
  bottom: 22rpx;
  width: 6rpx;
  border-radius: 999rpx;
  background: linear-gradient(180deg, var(--brand), var(--brand-warm));
  opacity: 0.72;
}

.compact-list {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.field-icon {
  width: 18rpx;
  height: 18rpx;
  border-radius: 50%;
  flex-shrink: 0;
}

.field-icon.start {
  background: #20bf6b;
  box-shadow: 0 0 0 10rpx rgba(32, 191, 107, 0.12);
}

.field-icon.end {
  background: #ff5b57;
  box-shadow: 0 0 0 10rpx rgba(255, 91, 87, 0.12);
}

.field-body {
  flex: 1;
  min-width: 0;
}

.field-label {
  font-size: 22rpx;
  color: var(--text-muted);
}

.field-value {
  margin-top: 8rpx;
  font-size: 28rpx;
  font-weight: 600;
  color: var(--text-primary);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.chip-row {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
}

.chip {
  padding: 14rpx 24rpx;
  border-radius: 999rpx;
  font-size: 24rpx;
  color: var(--text-secondary);
  background: #ffffff;
  border: 1rpx solid rgba(31, 36, 50, 0.08);
  transition: transform 0.18s ease, background 0.18s ease, border-color 0.18s ease;
}

.chip-active {
  color: var(--brand);
  background: var(--brand-soft);
  border-color: rgba(255, 122, 0, 0.2);
  box-shadow: 0 8rpx 20rpx rgba(255, 122, 0, 0.1);
}

.status-tag {
  display: inline-flex;
  align-items: center;
  padding: 8rpx 16rpx;
  border-radius: 999rpx;
  font-size: 22rpx;
  font-weight: 600;
}

.status-tag.waiting {
  color: #c77700;
  background: rgba(255, 176, 32, 0.14);
}

.status-tag.success {
  color: #13885e;
  background: rgba(27, 169, 122, 0.14);
}

.status-tag.processing {
  color: #2c63ff;
  background: rgba(44, 99, 255, 0.12);
}

.status-tag.danger {
  color: #d13d39;
  background: rgba(255, 91, 87, 0.12);
}

.metric-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 18rpx;
}

.metric-item {
  padding: 24rpx 20rpx;
  border-radius: 24rpx;
  background: rgba(255, 255, 255, 0.96);
  border: 1rpx solid rgba(31, 36, 50, 0.05);
  box-shadow: var(--shadow-soft);
}

.metric-value {
  font-size: 34rpx;
  font-weight: 700;
}

.metric-label {
  margin-top: 8rpx;
  font-size: 22rpx;
  color: var(--text-secondary);
}

.list-gap {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.skeleton {
  position: relative;
  overflow: hidden;
  background: #eef1f7;
}

.skeleton::after {
  content: "";
  position: absolute;
  inset: 0;
  transform: translateX(-100%);
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.7), transparent);
  animation: shimmer 1.4s infinite;
}

.floating-bar {
  position: fixed;
  left: 24rpx;
  right: 24rpx;
  bottom: calc(env(safe-area-inset-bottom) + 24rpx);
  z-index: 40;
}

.sticky-action {
  position: sticky;
  bottom: calc(env(safe-area-inset-bottom) + 20rpx);
  z-index: 20;
  margin-top: 24rpx;
}

.mask-panel {
  position: fixed;
  inset: 0;
  background: rgba(15, 23, 42, 0.38);
  z-index: 60;
}

.bottom-sheet {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 61;
  border-radius: 36rpx 36rpx 0 0;
  background: #ffffff;
  padding: 28rpx 24rpx calc(env(safe-area-inset-bottom) + 30rpx);
  animation: slide-up 0.28s var(--motion-smooth);
}

.fade-in {
  animation: fade-in 0.24s var(--motion-smooth);
}

.slide-in {
  animation: slide-up 0.3s var(--motion-smooth);
}

.silky-page {
  opacity: 0;
  transform: translateY(8rpx);
}

.silky-page--enter {
  animation: silky-page-enter 0.3s var(--motion-smooth) forwards;
}

.silky-page--leave {
  animation: silky-page-leave 0.16s var(--motion-press) forwards;
}

.page-enter {
  animation: page-enter 0.32s var(--motion-smooth) both;
}

.page-leaving,
.page-enter.page-leaving,
.page-leaving .page-enter {
  pointer-events: none;
  animation: silky-page-leave 0.16s var(--motion-press) forwards;
  transform-origin: center top;
}

.card-hover,
.tap-card {
  transition:
    transform 0.16s var(--motion-press),
    opacity 0.16s var(--motion-press),
    box-shadow 0.22s var(--motion-smooth);
  will-change: transform, opacity;
}

.tap-scale {
  transition:
    transform 0.16s var(--motion-press),
    opacity 0.16s var(--motion-press),
    box-shadow 0.22s var(--motion-smooth);
  will-change: transform, opacity;
}

.tap-card:active,
.card-hover:active {
  transform: translateY(2rpx) scale(0.985);
  opacity: 0.96;
}

.pressable-row,
.pressable-surface {
  transition:
    transform 0.16s var(--motion-press),
    opacity 0.16s var(--motion-press),
    background 0.2s var(--motion-smooth),
    box-shadow 0.2s var(--motion-smooth);
  will-change: transform, opacity;
}

.badge-dot {
  width: 14rpx;
  height: 14rpx;
  border-radius: 50%;
  background: var(--brand);
}

@keyframes shimmer {
  100% {
    transform: translateX(100%);
  }
}

@keyframes slide-up {
  from {
    transform: translateY(100%);
    opacity: 0;
  }
  to {
    transform: translateY(0);
    opacity: 1;
  }
}

@keyframes fade-in {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

@keyframes silky-page-enter {
  from {
    opacity: 0;
    transform: translateY(8rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes silky-page-leave {
  from {
    opacity: 1;
    transform: translateY(0);
  }
  to {
    opacity: 0.84;
    transform: translateY(5rpx) scale(0.998);
  }
}

@keyframes page-enter {
  from {
    opacity: 0;
    transform: translateY(8rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@media (prefers-reduced-motion: reduce) {
  .page-enter,
  .silky-page--enter,
  .silky-page--leave,
  .page-leaving,
  .page-enter.page-leaving,
  .page-leaving .page-enter,
  .fade-in,
  .slide-in,
  .bottom-sheet,
  .skeleton::after {
    animation-duration: 0.01ms;
    transition-duration: 0.01ms;
  }
}
