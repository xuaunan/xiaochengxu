:root,
page {
  --brand: #ff7a00;
  --brand-soft: #fff1df;
  --brand-deep: #ff9326;
  --brand-warm: #ffb14a;
  --brand-weak: #fff8ef;
  --brand-ring: rgba(255, 122, 0, 0.16);
  --text-primary: #1f2432;
  --text-secondary: #6b7385;
  --text-muted: #98a1b3;
  --text-inverse: #ffffff;
  --line: rgba(31, 36, 50, 0.08);
  --line-strong: rgba(31, 36, 50, 0.12);
  --card: #ffffff;
  --page: #f7f8fc;
  --page-warm: #fff7ef;
  --success: #1ba97a;
  --warning: #ffb020;
  --danger: #ff5b57;
  --info: #2c63ff;
  --shadow-soft: 0 14rpx 36rpx rgba(24, 34, 56, 0.07);
  --shadow-card: 0 20rpx 52rpx rgba(24, 34, 56, 0.09);
  --shadow-brand: 0 16rpx 36rpx rgba(255, 122, 0, 0.2);
  --radius-sm: 16rpx;
  --radius-md: 22rpx;
  --radius-lg: 28rpx;
  --radius-xl: 34rpx;
  background: var(--page);
  color: var(--text-primary);
  font-size: 28rpx;
  font-family: "PingFang SC", "Helvetica Neue", sans-serif;
}

view,
text,
button,
input,
textarea,
scroll-view,
swiper,
swiper-item,
map {
  box-sizing: border-box;
}

input {
  min-height: 76rpx;
}

.search-input,
.login-input,
.auth-input {
  width: 100%;
  height: 76rpx;
  min-height: 76rpx;
  padding: 0 24rpx;
  border-radius: 24rpx;
  background: rgba(247, 248, 252, 0.92);
  border: 1rpx solid rgba(255, 122, 0, 0.08);
  font-size: 28rpx;
  line-height: 76rpx;
  color: var(--text-primary);
}

.search-input::placeholder,
.login-input::placeholder,
.auth-input::placeholder {
  color: var(--text-muted);
}

.page-shell {
  min-height: 100vh;
  background:
    radial-gradient(circle at 88% 4%, rgba(255, 177, 74, 0.2), transparent 31%),
    radial-gradient(circle at 8% 12%, rgba(255, 122, 0, 0.1), transparent 26%),
    linear-gradient(180deg, var(--page-warm) 0%, #fffaf4 18%, var(--page) 46%, var(--page) 100%);
}

.safe-bottom {
  padding-bottom: calc(env(safe-area-inset-bottom) + 40rpx);
}

.page-content {
  padding: 24rpx 24rpx 40rpx;
}

.page-hero {
  position: relative;
  overflow: hidden;
  padding: 30rpx 28rpx;
  border-radius: var(--radius-xl);
  background:
    linear-gradient(135deg, rgba(255, 122, 0, 0.94) 0%, rgba(255, 154, 45, 0.92) 58%, rgba(255, 190, 94, 0.9) 100%);
  color: var(--text-inverse);
  box-shadow: var(--shadow-brand);
}

.page-hero::after {
  content: "";
  position: absolute;
  width: 220rpx;
  height: 220rpx;
  right: -70rpx;
  top: -86rpx;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.18);
}

.page-hero__kicker,
.page-kicker {
  font-size: 22rpx;
  font-weight: 700;
  letter-spacing: 1rpx;
}

.page-hero__title,
.page-title {
  margin-top: 10rpx;
  font-size: 40rpx;
  font-weight: 800;
  line-height: 1.22;
}

.page-hero__desc,
.page-desc {
  margin-top: 12rpx;
  font-size: 24rpx;
  line-height: 1.65;
}

.page-hero__desc {
  max-width: 580rpx;
  color: rgba(255, 255, 255, 0.86);
}

.page-header {
  padding: 10rpx 4rpx 18rpx;
}

.page-kicker {
  color: var(--brand);
}

.page-title {
  color: var(--text-primary);
}

.page-desc {
  color: var(--text-secondary);
}

.home-page {
  position: relative;
  min-height: 100vh;
  background: #f7f8fc;
}

.home-map {
  width: 100%;
  height: 100vh;
}

.home-overlay {
  position: absolute;
  left: 0;
  right: 0;
  top: 0;
  z-index: 10;
}

.hero-card,
.panel-card,
.glass-card {
  background: var(--card);
  border: 1rpx solid rgba(255, 255, 255, 0.76);
  border-radius: var(--radius-xl);
  box-shadow: var(--shadow-card);
}

.hero-card {
  padding: 28rpx;
}

.panel-card {
  padding: 24rpx;
}

.glass-card {
  background: rgba(255, 255, 255, 0.88);
  backdrop-filter: blur(14rpx);
  padding: 24rpx;
}

.section-title {
  font-size: 36rpx;
  font-weight: 800;
  color: var(--text-primary);
  line-height: 1.28;
}

.section-subtitle {
  margin-top: 10rpx;
  font-size: 24rpx;
  line-height: 1.65;
  color: var(--text-secondary);
}

.grid-2 {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 20rpx;
}

.row-between {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.row-center {
  display: flex;
  align-items: center;
}

.text-primary {
  color: var(--text-primary);
}

.text-secondary {
  color: var(--text-secondary);
}

.text-muted {
  color: var(--text-muted);
}

.brand-text {
  color: var(--brand);
}

.pill {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 10rpx 20rpx;
  border-radius: 999rpx;
  font-size: 22rpx;
  background: var(--brand-soft);
  color: var(--brand);
  line-height: 1.2;
}

.soft-pill {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 8rpx 16rpx;
  border-radius: 999rpx;
  font-size: 21rpx;
  font-weight: 700;
  color: var(--brand);
  background: rgba(255, 122, 0, 0.1);
}

.divider {
  height: 1rpx;
  background: var(--line);
}

.primary-button,
.ghost-button,
.warn-button {
  border: none;
  border-radius: 28rpx;
  font-size: 30rpx;
  font-weight: 600;
  line-height: 1;
  padding: 26rpx 28rpx;
  transition: transform 0.2s ease, opacity 0.2s ease;
}

.primary-button::after,
.ghost-button::after,
.warn-button::after {
  border: none;
}

.primary-button {
  color: #ffffff;
  background: linear-gradient(135deg, var(--brand) 0%, var(--brand-deep) 100%);
  box-shadow: 0 14rpx 32rpx rgba(255, 122, 0, 0.22);
}

.ghost-button {
  color: var(--brand);
  background: var(--brand-soft);
}

.warn-button {
  color: #ffffff;
  background: linear-gradient(135deg, #ff7f68 0%, #ff5b57 100%);
}

.primary-button.button-hover,
.ghost-button.button-hover,
.warn-button.button-hover,
.tap-scale:active,
.pressable-hover {
  transform: scale(0.98);
  opacity: 0.96;
}

.field-card {
  display: flex;
  align-items: center;
  gap: 20rpx;
  padding: 22rpx 24rpx;
  background: rgba(247, 248, 252, 0.9);
  border: 1rpx solid rgba(255, 122, 0, 0.08);
  border-radius: 24rpx;
}

.form-card {
  padding: 24rpx;
  border-radius: var(--radius-lg);
  background: rgba(255, 255, 255, 0.92);
  border: 1rpx solid rgba(255, 122, 0, 0.08);
  box-shadow: var(--shadow-soft);
}

.form-label {
  margin-bottom: 12rpx;
  font-size: 24rpx;
  font-weight: 700;
  color: var(--text-primary);
}

.form-help {
  margin-top: 10rpx;
  font-size: 22rpx;
  line-height: 1.55;
  color: var(--text-muted);
}

.list-card {
  position: relative;
  overflow: hidden;
  padding: 24rpx;
  border-radius: var(--radius-lg);
  background: rgba(255, 255, 255, 0.94);
  border: 1rpx solid rgba(31, 36, 50, 0.06);
  box-shadow: var(--shadow-soft);
}

.list-card::before {
  content: "";
  position: absolute;
  left: 0;
  top: 22rpx;
  bottom: 22rpx;
  width: 6rpx;
  border-radius: 999rpx;
  background: linear-gradient(180deg, var(--brand), var(--brand-warm));
  opacity: 0.72;
}

.compact-list {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.field-icon {
  width: 18rpx;
  height: 18rpx;
  border-radius: 50%;
  flex-shrink: 0;
}

.field-icon.start {
  background: #20bf6b;
  box-shadow: 0 0 0 10rpx rgba(32, 191, 107, 0.12);
}

.field-icon.end {
  background: #ff5b57;
  box-shadow: 0 0 0 10rpx rgba(255, 91, 87, 0.12);
}

.field-body {
  flex: 1;
  min-width: 0;
}

.field-label {
  font-size: 22rpx;
  color: var(--text-muted);
}

.field-value {
  margin-top: 8rpx;
  font-size: 28rpx;
  font-weight: 600;
  color: var(--text-primary);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.chip-row {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
}

.chip {
  padding: 14rpx 24rpx;
  border-radius: 999rpx;
  font-size: 24rpx;
  color: var(--text-secondary);
  background: #ffffff;
  border: 1rpx solid rgba(31, 36, 50, 0.08);
  transition: transform 0.18s ease, background 0.18s ease, border-color 0.18s ease;
}

.chip-active {
  color: var(--brand);
  background: var(--brand-soft);
  border-color: rgba(255, 122, 0, 0.2);
  box-shadow: 0 8rpx 20rpx rgba(255, 122, 0, 0.1);
}

.status-tag {
  display: inline-flex;
  align-items: center;
  padding: 8rpx 16rpx;
  border-radius: 999rpx;
  font-size: 22rpx;
  font-weight: 600;
}

.status-tag.waiting {
  color: #c77700;
  background: rgba(255, 176, 32, 0.14);
}

.status-tag.success {
  color: #13885e;
  background: rgba(27, 169, 122, 0.14);
}

.status-tag.processing {
  color: #2c63ff;
  background: rgba(44, 99, 255, 0.12);
}

.status-tag.danger {
  color: #d13d39;
  background: rgba(255, 91, 87, 0.12);
}

.metric-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 18rpx;
}

.metric-item {
  padding: 24rpx 20rpx;
  border-radius: 24rpx;
  background: linear-gradient(180deg, rgba(255, 255, 255, 0.98), rgba(255, 246, 236, 0.92));
}

.metric-value {
  font-size: 34rpx;
  font-weight: 700;
}

.metric-label {
  margin-top: 8rpx;
  font-size: 22rpx;
  color: var(--text-secondary);
}

.list-gap {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.skeleton {
  position: relative;
  overflow: hidden;
  background: #eef1f7;
}

.skeleton::after {
  content: "";
  position: absolute;
  inset: 0;
  transform: translateX(-100%);
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.7), transparent);
  animation: shimmer 1.4s infinite;
}

.floating-bar {
  position: fixed;
  left: 24rpx;
  right: 24rpx;
  bottom: calc(env(safe-area-inset-bottom) + 24rpx);
  z-index: 40;
}

.sticky-action {
  position: sticky;
  bottom: calc(env(safe-area-inset-bottom) + 20rpx);
  z-index: 20;
  margin-top: 24rpx;
}

.mask-panel {
  position: fixed;
  inset: 0;
  background: rgba(15, 23, 42, 0.38);
  z-index: 60;
}

.bottom-sheet {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 61;
  border-radius: 36rpx 36rpx 0 0;
  background: #ffffff;
  padding: 28rpx 24rpx calc(env(safe-area-inset-bottom) + 30rpx);
  animation: slide-up 0.25s ease;
}

.fade-in {
  animation: fade-in 0.3s ease;
}

.slide-in {
  animation: slide-up 0.28s ease;
}

.page-enter {
  animation: page-enter 0.34s ease both;
}

.card-hover,
.tap-card {
  transition: transform 0.2s ease, opacity 0.2s ease, box-shadow 0.2s ease;
}

.tap-card:active,
.card-hover:active {
  transform: translateY(2rpx) scale(0.99);
  opacity: 0.96;
}

.badge-dot {
  width: 14rpx;
  height: 14rpx;
  border-radius: 50%;
  background: var(--brand);
}

@keyframes shimmer {
  100% {
    transform: translateX(100%);
  }
}

@keyframes slide-up {
  from {
    transform: translateY(100%);
    opacity: 0;
  }
  to {
    transform: translateY(0);
    opacity: 1;
  }
}

@keyframes fade-in {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

@keyframes page-enter {
  from {
    opacity: 0;
    transform: translateY(18rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
